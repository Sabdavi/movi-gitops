apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "movi-api.fullname" . }}-test-connection"
  labels:
    {{- include "movi-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "movi-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
